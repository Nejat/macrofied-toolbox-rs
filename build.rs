use std::io;

pub mod build_macros;

// use build_macros::generate_result_macro;
// use build_macros::generate_result_macro_tests;

fn main() -> io::Result<()> {
    // generate_result_macro()?;
    // generate_result_macro_tests()?;

    Ok(())
}
